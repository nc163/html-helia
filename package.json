{
  "name": "ipfs-tag",
  "description": "ipfs-tag is a library for fetch data from IPFS and displaying it on a web page.",
  "version": "0.0.7",
  "homepage": "https://github.com/nc163/ipfs-tag",
  "bugs": {
    "url": "https://github.com/nc163/ipfs-tag/issues"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "http://github.com/nc163/ipfs-tag.git"
  },
  "module": "esm/index.js",
  "type": "module",
  "scripts": {
    "dev": " npm run build:umd && node main.js",
    "build": "npm run build:esm && npm run build:umd",
    "build:esm": "rimraf esm && tsc -p tsconfig.esm.json",
    "build:umd": "rimraf umd && tsc -p tsconfig.umd.json"
  },
  "files": [
    "esm"
  ],
  "keywords": [
    "browser",
    "ipfs",
    "dapps"
  ],
  "dependencies": {
    "ipfs-core": "^0.18.0",
    "media-type": "^0.3.1",
    "multiformats": "^10.0.2"
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/node": "^18.16.3",
    "express": "^4.18.2",
    "rimraf": "^5.0.0",
    "ts-node": "^10.9.1",
    "typescript": "^5.0.4"
  }
}
