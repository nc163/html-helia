{
  "name": "@ipfs-tag/html-ipfs-element",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "rimraf dist && tsc -p tsconfig.json",
    "browser": "rimraf example/public/js && browserify src/*.ts -p [ tsify --project tsconfig.browserify.json ] -d -o example/public/js/bundle.js",
    "browser2": "rimraf tmp && tsc -p tsconfig.browserify.json && rimraf example/public/js && browserify tmp/*.js -d -o example/public/js/bundle.js"
  },
  "dependencies": {
    "@helia/unixfs": "^1.4.1",
    "helia": "^2.0.1"
  },
  "devDependencies": {
    "@helia/interface": "^2.0.0",
    "browserify": "^17.0.0",
    "tsify": "^5.0.4",
    "tslib": "^2.5.0",
    "typescript": "^5.0.4"
  },
  "private": false,
  "author": "nc163",
  "license": "ISC",
  "browser": {"process" : false}
}
